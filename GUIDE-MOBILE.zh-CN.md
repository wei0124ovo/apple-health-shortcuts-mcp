# 只用手机：让 AI 看懂 Apple 健康

## 中国时区默认版｜不装软件｜不碰代码

版本：2.0（2026 年 7 月）

> 这版不需要电脑。Cloudflare 会自动创建数据库，手机设置页会自动生成钥匙。你只需要点网页、填一个部署密码，再在 iPhone 安装或制作两个快捷指令。

---

## 先看懂这条路

```text
Apple Watch
    ↓
iPhone“健康”
    ↓ 快捷指令每天送一次
你自己的 Cloudflare
    ↓ AI 需要时读取
3 个只读健康工具
```

默认时区已经是中国：

```text
Asia/Shanghai
```

不用改文件，不用填写数据库编号，不用运行任何命令。

---

## 你需要什么

- 一台 iPhone。
- Apple Watch 可有可无。
- 一个免费的 GitHub 账号。
- 一个免费的 Cloudflare 账号。
- 一个能添加 HTTP MCP 的 AI 软件。

手机上的 Safari 就够了。

> 中国大陆访问 GitHub、Cloudflare 或 `workers.dev` 偶尔可能慢或失败。这是网络连接问题，不代表你填错。免费方案无法保证所有地区、所有运营商一直畅通。

---

## 第一部分：手机一键放上云

### 1. 点一键部署

用 Safari 打开：

[点这里部署到 Cloudflare](https://deploy.workers.cloudflare.com/?url=https://github.com/shenqingmo3-dotcom/apple-health-shortcuts-mcp)

它会让你登录 GitHub 和 Cloudflare。

Cloudflare 会自动做这些事情：

- 复制一份代码到你自己的 GitHub。
- 创建一个只属于你的 Worker。
- 创建一个只属于你的 D1 健康数据小抽屉。
- 连接数据库。
- 建好数据表。

这些都不需要你看懂。

### 2. 名字全部使用默认

页面如果让你填写：

- 仓库名称；
- Worker 名称；
- 数据库名称；

都可以直接使用它给出的默认名字。

不要把别人的 Worker 或数据库拿来共用。

### 3. 填“部署密码”

页面会出现：

```text
SETUP_KEY
```

这里填一串只有你知道的密码，至少 12 个字符。

可以用：

```text
Cloud小猫-今天吃了7个苹果!
```

这只是格式例子，请不要照抄。

不要使用：

- `123456`
- 生日
- 手机号
- 群里所有人都知道的词

把部署密码存进 iPhone“密码”App 或私人备忘录。

### 4. 点 Deploy

点“Deploy”或“部署”，等待页面显示成功。

手机不要立刻锁屏。通常需要几十秒到几分钟。

成功后会出现一个类似这样的地址：

```text
https://你的名字.你的账号.workers.dev
```

这叫“我的云地址”。

---

## 第二部分：手机领取两把钥匙

### 5. 打开设置页

在“我的云地址”后面加：

```text
/setup
```

例如：

```text
https://你的名字.你的账号.workers.dev/setup
```

用 Safari 打开。

### 6. 输入部署密码

输入刚才的 `SETUP_KEY`，点“生成两把钥匙”。

页面会给出四项：

1. iPhone 上传地址。
2. iPhone 上传钥匙。
3. AI 的 MCP 地址。
4. AI 的 Bearer Token。

每项旁边都有“复制”按钮。

把四项存进 iPhone“密码”App或私人备忘录。

### 为什么有两把钥匙？

- 第一把只能把健康数据送进去。
- 第二把只能让 AI 读取。

AI 拿不到上传钥匙，快捷指令也拿不到 AI 钥匙。

### 忘记钥匙怎么办？

重新打开：

```text
我的云地址/setup
```

输入部署密码，重新生成。

旧钥匙会立刻失效，所以 iPhone 和 AI 里的钥匙也要一起换。

---

## 第三部分：安装 iPhone 快捷指令

这里有两条路。

### 最简单：群主提供了 iCloud 链接

如果群主已经做好快捷指令：

1. 点“上传健康给 AI”的 iCloud 链接。
2. 点“添加快捷指令”。
3. 它询问“上传地址”时，粘贴设置页的第 1 项。
4. 它询问“上传钥匙”时，粘贴设置页的第 2 项。
5. 再用同样方法安装“上传睡眠给 AI”。
6. 第一次运行时，允许读取健康数据与联网。

这样就不用自己搭积木。

> 本项目不能在 Windows 上伪造 Apple 签名的快捷指令。iCloud 链接必须由一个真实 iPhone 用户创建并分享。下文会教群主只做一次。

### 没有 iCloud 链接：在手机手工做

先做最小版，只上传心率。确认成功后再添加其他数据。

---

## 第四部分：手工做“上传健康给 AI”

### 7. 新建快捷指令

1. 打开“快捷指令”。
2. 点右上角 `+`。
3. 名字改成“上传健康给 AI”。

### 8. 查找今天的心率

1. 添加“查找健康样本”。
2. 类型选择“心率”。
3. 条件选择“开始日期是今天”。
4. 排序选择“开始日期、最新优先”。
5. 打开“限制”，数量填 `20`。

按钮名字可能因为 iOS 版本差一两个字，选意思相近的即可。

### 9. 每条心率做一张小卡片

添加“重复每一项”。在重复框里面：

1. 获取“重复项目”的“值”。
2. 获取“重复项目”的“开始日期”。
3. 把开始日期格式化为 `ISO 8601`。
4. 添加“字典”。

字典放四行：

| 左边名字 | 右边内容 |
|---|---|
| `type` | 文字 `heart_rate` |
| `value` | 刚取出的值 |
| `at` | 格式化后的日期 |
| `unit` | 文字 `bpm` |

把字典添加到变量“健康小卡片”。

### 10. 把小卡片寄出去

在“结束重复”下面：

1. 添加“URL”，粘贴设置页第 1 项“iPhone 上传地址”。
2. 添加“获取 URL 内容”。
3. 方法选择 `POST`。
4. 请求正文选择 `JSON`。
5. JSON 添加 `metrics`，内容选择变量“健康小卡片”。
6. 请求头添加 `X-Upload-Key`。
7. 请求头内容粘贴设置页第 2 项“iPhone 上传钥匙”。
8. 最后添加“显示结果”。

运行后看到：

```text
"ok": true
```

就成功了。

### 11. 添加其他数据

复制“心率”这一小块，每次只改类型、`type` 和单位：

| 健康类型 | `type` | 单位 |
|---|---|---|
| 心率 | `heart_rate` | `bpm` |
| 心率变异性 | `heart_rate_variability` | `ms` |
| 静息心率 | `resting_heart_rate` | `bpm` |
| 呼吸速率 | `respiratory_rate` | `breaths/min` |
| 血氧 | `blood_oxygen_saturation` | `%` |
| 步数 | `step_count` | `count` |
| 活动能量 | `active_energy_burned` | `kcal` |
| 锻炼时间 | `apple_exercise_time` | `min` |
| 手腕温度 | `wrist_temperature` | `°C` |

找不到的项目直接跳过。

---

## 第五部分：手工做“上传睡眠给 AI”

### 12. 找出最近睡眠

1. 新建快捷指令，名字叫“上传睡眠给 AI”。
2. 添加“查找健康样本”。
3. 类型选择“睡眠”或“睡眠分析”。
4. 条件选择过去 18 小时。

没有“过去 18 小时”时：

1. 添加“当前日期”。
2. 添加“调整日期”，减去 18 小时。
3. 查找开始日期晚于“调整后的日期”的睡眠样本。

### 13. 做睡眠小卡片

添加“重复每一项”。在重复框里分别取得：

- 值或睡眠阶段。
- 开始日期。
- 结束日期。

把开始和结束都格式化成 `ISO 8601`。

添加字典：

| 左边名字 | 右边内容 |
|---|---|
| `stage` | 值或睡眠阶段 |
| `start` | 格式化后的开始日期 |
| `end` | 格式化后的结束日期 |

把字典加入变量“睡眠小卡片”。

### 14. 上传睡眠

在“结束重复”下面：

- URL：设置页第 1 项。
- 方法：`POST`。
- 正文：`JSON`。
- JSON 名字：`sleep`。
- JSON 内容：变量“睡眠小卡片”。
- 请求头名字：`X-Upload-Key`。
- 请求头内容：设置页第 2 项。

运行后看到 `"ok": true` 即成功。

---

## 第六部分：让它每天自己跑

### 15. 醒来时上传睡眠

1. 快捷指令底部点“自动化”。
2. 点 `+`。
3. 选择“睡眠”或“起床”。
4. 选择“醒来时”。
5. 运行“上传睡眠给 AI”。
6. 选择“立即运行”。

### 16. 晚上上传状态

1. 新建“特定时间”自动化。
2. 时间例如每天 `21:30`。
3. 运行“上传健康给 AI”。
4. 选择“立即运行”。

一天运行一两次即可，不要每分钟上传。

锁屏、省电、没网或系统繁忙时，iPhone 可能推迟运行。把快捷指令放进桌面小组件，可以随时补点一次。

---

## 第七部分：把 MCP 交给 AI

填入设置页的第 3、4 项：

| AI 设置 | 填什么 |
|---|---|
| 名称 | `我的 Apple 健康` |
| MCP 地址 | 设置页第 3 项 |
| 认证方式 | `Bearer Token` |
| Token | 设置页第 4 项 |

若 AI 只让你添加请求头：

| 请求头 | 内容 |
|---|---|
| `Authorization` | `Bearer 第四项钥匙` |

连接成功应看到：

```text
health_now
health_detail
health_trends
```

AI 一般先调用一次 `health_now`。只有你继续问睡眠细节或长期变化时，才需要另外两个工具。

---

## 第八部分：群主怎样做成“点链接安装”

这部分只需群主做一次。

### 17. 给快捷指令加导入问题

打开已经测试成功的快捷指令：

1. 打开快捷指令详情。
2. 点“设置”或 `Setup`。
3. 点“添加新问题”。
4. 选择 URL 里的上传地址。
5. 问题写“请粘贴 iPhone 上传地址”。
6. 再给 `X-Upload-Key` 的内容添加一个问题。
7. 问题写“请粘贴 iPhone 上传钥匙”。

睡眠快捷指令也这样做。

导入问题会清掉群主自己的地址和钥匙，避免把隐私分享出去。

### 18. 分享 iCloud 链接

1. 在快捷指令编辑页点“分享”。
2. 选择“复制 iCloud 链接”。
3. 把两个链接发到群里。

以后群友只需：

```text
手机一键部署 → 打开 /setup → 点两个 iCloud 链接 → 填两项
```

全程不需要电脑。

---

## 优点

- 中国时区已经设好。
- 不安装 Node，不打开终端，不改代码。
- 数据库自动创建。
- 钥匙由手机网页自动生成。
- 每个人都有自己的云端、数据库和钥匙。
- 群主做好 iCloud 链接后，其他人不必手工搭快捷指令。
- AI 只有 3 个只读工具。

## 缺点

- 仍需注册 GitHub 与 Cloudflare。
- 第一次部署要等待 Cloudflare 构建。
- 群主必须用一台真实 iPhone 做好并分享快捷指令。
- 中国大陆访问 GitHub、Cloudflare、`workers.dev` 可能不稳定。
- iPhone 自动化偶尔会延迟或漏跑，不是严格实时。
- 健康数据存放在第三方云端。
- 免费额度和平台规则以后可能变化。
- 不能用于诊断、急救或替代医生。

---

## 最后检查

- [ ] Cloudflare 部署显示成功。
- [ ] `我的云地址/healthz` 显示 `ok: true`。
- [ ] `/setup` 成功生成四项内容。
- [ ] 心率上传显示 `ok: true`。
- [ ] 睡眠上传显示 `ok: true`。
- [ ] 两个自动化选择“立即运行”。
- [ ] AI 看到 3 个工具。
- [ ] 没有把部署密码或钥匙发到群里。

全部打勾，就完成了。

---

## 官方依据

- [Cloudflare：一键部署会自动创建并绑定所需资源](https://developers.cloudflare.com/workers/platform/deploy-buttons/)
- [Apple：快捷指令可以查找健康样本](https://support.apple.com/en-nz/guide/shortcuts/apd3c845e881/ios)
- [Apple：快捷指令可以 POST JSON](https://support.apple.com/en-nz/guide/shortcuts/apd58d46713f/ios)
- [Apple：时间、睡眠自动化可无需确认运行](https://support.apple.com/en-ca/guide/shortcuts/apd602971e63/ios)
- [Apple：共享快捷指令可添加导入问题](https://support.apple.com/en-sg/guide/shortcuts/apdf330fd3a0/ios)
- [Apple：可复制 iCloud 链接分享快捷指令](https://support.apple.com/en-il/guide/shortcuts/apdf01f8c054/ios)
